name: Feature Request
description: Request a new feature or enhancement
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Describe the feature clearly. Claude Code reads this to understand what to build.

  - type: textarea
    id: description
    attributes:
      label: Feature description
      description: What should this feature do?
      placeholder: "Add a timer that automatically closes a poll after a set date."
    validations:
      required: true

  - type: textarea
    id: user_story
    attributes:
      label: User story
      description: Who benefits and how?
      placeholder: |
        As a site admin, I want to set an expiry date on a poll,
        so that voting closes automatically without manual intervention.
    validations:
      required: true

  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: Explicit conditions that must be true for this feature to be complete. Used as definition of done.
      placeholder: |
        - [ ] Block editor shows a date/time picker in the poll block settings
        - [ ] After the expiry date, the Vote button is replaced with "Poll closed"
        - [ ] Existing votes are still visible after poll closes
    validations:
      required: true

  - type: textarea
    id: out_of_scope
    attributes:
      label: Out of scope
      description: What should NOT be included in this implementation?
      placeholder: "Email notifications when poll closes — separate issue."

  - type: textarea
    id: extra
    attributes:
      label: Additional context / mockups
      description: Wireframes, screenshots, or references.
